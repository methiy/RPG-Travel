# Required: Secret key for signing auth cookies
# Generate with: openssl rand -hex 32
AUTH_SECRET=your-secret-here

# Required: Vercel Postgres connection URL
# Create a Postgres database in Vercel Dashboard → Storage, then link it to your project.
# These environment variables are automatically injected when linked.
POSTGRES_URL=your-postgres-url-here
